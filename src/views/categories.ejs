<%- include('partials/header') %>

    <main>

        <h1>
            <%= title %>
        </h1>

        <% if (message) { %>
            <p class="flash-message">
                <%= message %>
            </p>
            <% } %>

        <a href="/new-category" class="btn">
            Create New Category
        </a>

        <ul>

            <% categories.forEach(category=> { %>

                <li>

                    <a href="/category/<%= category.category_id %>">

                        <%= category.name %>

                    </a>

                </li>

                <% }) %>

        </ul>

    </main>

    <%- include('partials/footer') %>